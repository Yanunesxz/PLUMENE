/// <reference types="vite/client" />
/// <reference types="vite-plugin-pwa/client" />

/** Momento do build (ISO). Injetado pelo Vite — ver `define` em vite.config.ts. */
declare const __VERSAO_BUILD__: string;
